{
  "name": "twentyfour-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:push": "wrangler d1 execute twentyfour-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.84.0",
    "typescript": "^5.5.4"
  }
}